# ТЗ v1.0 — Дослідження IBM i2 Analyst Notebook
## Наступний цикл розробки: зручність малювання схем

**Дата:** 2026-05-19  
**Джерело:** Аналіз IBM i2 AN 9.x + поточний `main.py` (3724 рядки)  
**Фокус:** Що робить i2 галузевим стандартом → що впроваджуємо далі

---

## Ключові переваги IBM i2 (чому аналітики його люблять)

IBM i2 Analyst Notebook — інструмент розслідування для спецслужб, поліції, фінансової розвідки.  
Його сила не в красивому UI, а в **швидкості побудови схем** і **читабельності при друку**.

Три речі, які виділяють i2:
1. **Семантичні зв'язки** — кожен зв'язок має тип, підпис, дату, силу (strong/weak), напрямок
2. **Гнучке оформлення** — кольори, іконки, товщина ліній, пунктир — все без діалогів
3. **Часова вісь** — можна прив'язати події до дат і бачити хронологію

---

## ПРІОРИТЕТ 1 — Критично для схем розслідування

### 1.1 Типи зв'язків з візуальним стилем
**Чого немає зараз:** Зв'язки мають тип (solid/dashed/dotted) але товщина і колір — завжди за замовчуванням  
**Що зробити:**
- Поле "Колір лінії" у `LinkDialog` → `QColorDialog`
- Поле "Товщина" → `QSpinBox` (1–5)
- Попередній перегляд стилю лінії в діалозі
- Зберігати `color` і `width` у моделі `Link`

**Технічно:** `LinkItem.paint()` вже малює лінію через `QPen` — просто передати `color` і `width`  
**Критерій:** Зв'язок між "особою" і "організацією" можна зробити червоним і товстим

---

### 1.2 Inline зміна кольору вузла (один клік)
**Чого немає зараз:** Зміна кольору — тільки через контекстне меню → діалог → підтвердження  
**Що зробити:**
- Кнопка кольору прямо в бічній панелі (кольоровий квадрат-кнопка)
- Клік → `QColorDialog` → одразу застосовується

**Технічно:** `ColorNodeCommand` вже є — просто додати кнопку у `PropertiesPanel`  
**Критерій:** Зміна кольору — 2 кліки, без діалогу "Редагувати…"

---

### 1.3 Мітки на зв'язках — відображення на схемі
**Чого немає зараз:** Підпис зв'язку зберігається але відображається дрібно або не по центру  
**Що перевірити:** `LinkItem.paint()` — як рендериться `link.label`  
**Що зробити:** Підпис завжди по центру лінії, читабельний шрифт, фон-прямокутник під текстом  
**Критерій:** На схемі з 10 зв'язками всі підписи читабельні без наближення

---

## ПРІОРИТЕТ 2 — Суттєве для зручності малювання

### 2.1 Групування вузлів (Containers / Frames)
**Що це в i2:** "Frame" — напівпрозорий прямокутник навколо групи вузлів з підписом.  
Переміщення рамки → всі вузли всередині рухаються разом  
**Що зробити:**
- `Edit → Згрупувати` або ПКМ → "Група"
- `GroupItem(QGraphicsRectItem)` — напівпрозорий, з підписом зверху
- При переміщенні групи → `ItemIsMovable` + зсув усіх дочірніх вузлів
- Розгрупування: `Edit → Розгрупувати`

**Технічно:** Qt підтримує батьківсько-дочірні відносини через `item.setParentItem(group)`  
**Критерій:** Виділив 3 вузли → "Група" → рамка → перетягнув цілком → всі вузли рухаються

---

### 2.2 Snap-to-Grid (прив'язка до сітки)
**Що це в i2:** При переміщенні вузол "прилипає" до найближчого вузла сітки при відпусканні  
**Що зробити:**
- Чекбокс у `Вид → Прив'язка до сітки`
- В `NodeItem.mouseReleaseEvent` або `itemChange(ItemPositionChange)`: округлити pos до кратного 30

**Технічно:**
```python
def itemChange(self, change, value):
    if change == QGraphicsItem.GraphicsItemChange.ItemPositionChange and snap_enabled:
        x = round(value.x() / 30) * 30
        y = round(value.y() / 30) * 30
        return QPointF(x, y)
    return super().itemChange(change, value)
```
**Критерій:** При увімкненій прив'язці вузли завжди стають рівно на лінії сітки

---

### 2.3 Авто-розміщення вузлів
**Що це в i2:** "Auto Layout" — алгоритм розкидує вузли щоб мінімізувати перетини ребер  
**Що зробити (спрощено):**
- `Вид → Авто-розміщення → По сітці` — рівномірно N×M
- `Вид → Авто-розміщення → Радіально` — центральний вузол + решта по колу

**Технічно:** Чиста математика — не потребує сторонніх бібліотек  
**Критерій:** 20 вузлів → авто-розміщення → схема читабельна, без перетинів

---

### 2.4 Іконки для типів вузлів (замість тексту)
**Що це в i2:** Кожен тип має характерну іконку (person = силует, org = будівля, phone = трубка)  
**Зараз у нас:** Букви-емодзі (`👤`, `🏢` тощо) — спрацьовує, але виглядає непрофесійно  
**Що зробити:**
- Завантажити або намалювати SVG/PNG іконки для 7 типів (16×16 або 24×24)
- `NodeItem.paint()` — малювати `QPixmap` іконку замість тексту
- Розмістити іконки у `assets/icons/`

**Критерій:** Тип вузла розпізнається по іконці без підпису

---

## ПРІОРИТЕТ 3 — Полірування

### 3.1 Часова вісь (Timeline)
**Що це в i2:** Окремий вид — вузли розташовуються по горизонтальній шкалі часу згідно `node.date`  
**Складність:** Висока — фактично окремий режим перегляду  
**Що зробити:** Окремий `QWidget` з горизонтальною шкалою, вузли-точки на ній  
**Критерій:** Перемикач "Схема / Хронологія" — в хронологічному режимі видно послідовність подій

---

### 3.2 Друк зі збереженням пропорцій
**Що це в i2:** "Print Preview" з вибором розміру паперу і орієнтації, масштабом  
**Що зробити:** `QPrintPreviewDialog` — вже є в PyQt6  
**Критерій:** Меню `Файл → Попередній перегляд друку` → вікно з прев'ю

---

### 3.3 Фільтр "показати тільки пов'язані"
**Що це в i2:** Клацнув вузол → `ПКМ → Show connected only` → приховує всі непов'язані вузли  
**Що зробити:** Пункт в контекстному меню вузла → тимчасово ховати `item.setVisible(False)` для непов'язаних  
**Критерій:** ПКМ → "Показати тільки зв'язані" → схема стає менш перевантаженою

---

### 3.4 Рефакторинг main.py на модулі
*(без зміни функціоналу — тільки після стабілізації всього вище)*

| Файл | Що туди іде |
|------|-------------|
| `models.py` | `Node`, `Link`, константи |
| `commands.py` | Всі `QUndoCommand` підкласи |
| `items.py` | `NodeItem`, `LinkItem`, `ShapeItem` |
| `dialogs.py` | `NodeDialog`, `LinkDialog`, `SearchPanel` |
| `canvas.py` | `DiagramCanvas`, `MinimapView` |
| `panels.py` | `PropertiesPanel`, `FilterPanel` |
| `main.py` | Тільки `MainWindow` + `if __name__ == '__main__'` |

---

## Пріоритизований план (наступний цикл)

| # | Задача | P | Складність | Критерій |
|---|--------|---|------------|---------|
| 1 | Стиль зв'язків (колір + товщина) | P1 | Низька | Червона товста лінія |
| 2 | Кнопка кольору в панелі | P1 | Низька | 2 кліки на зміну кольору |
| 3 | Підписи зв'язків читабельні | P1 | Середня | Текст по центру лінії |
| 4 | Групування вузлів | P2 | Висока | Рамка рухається з вузлами |
| 5 | Snap-to-Grid | P2 | Низька | Вузли на лінії сітки |
| 6 | Авто-розміщення | P2 | Середня | 20 вузлів без перетинів |
| 7 | Іконки типів | P2 | Середня | Силует замість букви |
| 8 | Часова вісь | P3 | Висока | Хронологічний режим |
| 9 | Друк з прев'ю | P3 | Низька | QPrintPreviewDialog |
| 10 | Рефакторинг | P3 | Висока | Всі тести зелені |

---

## Що вже є (підтверджено в main.py 3724 рядки)

✅ 7 типів вузлів + іконки-емодзі  
✅ Зв'язки 3 типів (solid/dashed/dotted) з підписом  
✅ Inline редагування назви і примітки в панелі *(додано 2026-05-19)*  
✅ Shape Tool (лінія, коло, прямокутник, текст) *(додано 2026-05-19)*  
✅ Мінімапа, Zoom під курсором, PDF експорт  
✅ CSV імпорт, PNG експорт, Undo/Redo, Пошук, Фільтр  

---

*Сформовано: Claude (аналіз IBM i2 AN 9.x docs + main.py) — 2026-05-19*
